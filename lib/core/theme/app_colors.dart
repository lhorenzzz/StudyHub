import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Dark Mode ────────────────────────────────────────────────────────────
  static const darkBg = Color(0xFF0D0D0D); // main background
  static const darkSurface = Color(0xFF111111); // cards, navbar
  static const darkSurface2 = Color(0xFF1A1A1A); // elevated elements
  static const darkBorder = Color(0xFF1F1F1F); // default borders
  static const darkBorder2 = Color(0xFF2A2A2A); // slightly visible borders
  static const darkText = Color(0xFFFFFFFF); // primary text
  static const darkTextSub = Color(0xFF555555); // secondary text
  static const darkTextMuted = Color(0xFF444444); // muted/hint text

  // ─── Light Mode ───────────────────────────────────────────────────────────
  static const lightBg = Color(0xFFF5F5F5); // main background
  static const lightSurface = Color(0xFFFFFFFF); // cards, navbar
  static const lightSurface2 = Color(0xFFF0F0F0); // elevated elements
  static const lightBorder = Color(0xFFE5E5E5); // default borders
  static const lightBorder2 = Color(0xFFDDDDDD); // slightly visible borders
  static const lightText = Color(0xFF111111); // primary text
  static const lightTextSub = Color(0xFF999999); // secondary text
  static const lightTextMuted = Color(0xFFBBBBBB); // muted/hint text
}
